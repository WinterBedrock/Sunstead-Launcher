<Application x:Class="SunsteadLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- сolors -->
            <Color x:Key="AccentColor">#FFF5A623</Color>
            <Color x:Key="AccentBrightColor">#FFFFC86B</Color>
            <Color x:Key="GlassColor">#FF0B0D14</Color>
            <Color x:Key="CardColor">#FF161926</Color>
            <Color x:Key="TextHiColor">#FFECEEF2</Color>
            <Color x:Key="TextMidColor">#FF969EB0</Color>
            <Color x:Key="GoodColor">#FF4ADE80</Color>
            <Color x:Key="BadColor">#FFF87171</Color>
            <Color x:Key="WarnColor">#FFFBBF24</Color>

            <SolidColorBrush x:Key="TextHi" Color="{StaticResource TextHiColor}" />
            <SolidColorBrush x:Key="TextMid" Color="{StaticResource TextMidColor}" />
            <SolidColorBrush x:Key="Good" Color="{StaticResource GoodColor}" />
            <SolidColorBrush x:Key="Bad" Color="{StaticResource BadColor}" />
            <SolidColorBrush x:Key="Warn" Color="{StaticResource WarnColor}" />

            <!-- fonts -->
            <FontFamily x:Key="ProductSans">Fonts\#Product Sans</FontFamily>
            <FontFamily x:Key="ProductSansBold">Fonts\#Product Sans Bold</FontFamily>

            <!-- window buttons -->
            <Style x:Key="WinButton" TargetType="Button">
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextMid}"/>
                <Setter Property="FontFamily" Value="{StaticResource ProductSansBold}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" CornerRadius="8" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#22FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextHi}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#33FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Close button turns red on hover -->
            <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WinButton}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" CornerRadius="8" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#EDEB4545"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- settings toggle -->
            <Style x:Key="SettingsButton" TargetType="ToggleButton">
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextMid}"/>
                <Setter Property="FontFamily" Value="{StaticResource ProductSansBold}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="bg" CornerRadius="8" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#22FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextHi}"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#33F5A623"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextHi}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
